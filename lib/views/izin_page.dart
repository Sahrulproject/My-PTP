import 'package:flutter/material.dart';

class IzinPage extends StatelessWidget {
  const IzinPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Izin Kehadiran Page', style: TextStyle(fontSize: 24)),
    );
  }
}
